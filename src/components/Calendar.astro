---
import * as R from "remeda";
import { randomString } from "remeda";
import { createAvatar } from "@dicebear/avatars";
import * as style from "@dicebear/open-peeps";

const { title } = Astro.props;

const getAvatar = (name: string) =>
  createAvatar(style, {
    seed: name,
    rotate: 270,
    scale: 25,
    accessoriesProbability: 0.8,
    skinColor: ["variant01", "variant02", "variant03", "variant04"],
  });

/** @example [18, 30] = 18:30 */
type Time = [number, number];

/**  @example [[18, 30], [19,30]] = 18:30-19:30 */
type TimeSpan = [Time, Time];

interface Slot {
  timeSpan: TimeSpan;
  who: string;
  phone: string;
}

const names = [
  "Чика",
  "Алмаз",
  "Эмир",
  "Улук",
  "Була",
  "Бека",
  "Анвар",
  "Замир байке",
  "Anish",
  "Gukesh",
  "Серёга",
  "Улушка",
];

const slots = R.pipe(
  R.range(10, 21),
  R.map<number, Slot>((it) => ({
    timeSpan: [
      [it, 0],
      [it + 1, 0],
    ],
    who: names[it % names.length],
    phone: randomString(10),
  }))
);
---

<div class="calendar">
  <h2>
    ✂️ {title ?? "cегодня"}
  </h2>
  <!-- <p style={{ fontSize: "40px", margin: 0, padding: 0 }}>🚪🚪🚪</p> -->
  <ul class="time-span-list">
    {
      slots.map((slot, i) => (
        <>
          {Math.random() > 0.5 ? (
            <li class="time-span">
              <span class="time">{slot.timeSpan[0][0]}</span>
              <div class="avatar" set:html={getAvatar(slot.who)} />
              <p class="name">({slot.who})</p>
            </li>
          ) : (
            <li class="empty">
              <span class="time">{slot.timeSpan[0][0]}</span>
              <a href="#"> записаться</a>
            </li>
          )}
        </>
      ))
    }
  </ul>
</div>

<style>
  .calendar {
    display: flex;
    flex-direction: column;
    min-width: 65vw;
    padding: 1rem;
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1),
      0 2px 4px -2px rgba(0, 0, 0, 0.1);
  }

  ul {
    display: flex;
    flex-direction: column;
    gap: 8px;
  }
  .time-span {
    display: flex;
    align-items: center;
    justify-content: flex-start;

    min-height: 60px;
    gap: 4px;
  }

  .empty {
    display: flex;
    padding: 10px 0px;
    gap: 4px;
  }

  .avatar {
    width: 72px;
    transform: scale(4);
  }

  .time {
    font-size: large;
    font-weight: bold;
  }

  .name {
    margin-left: auto;
    font-size: medium;
    font-variant-caps: petite-caps;
  }

  h2 {
    margin: 1rem 0.25rem;
    font-size: xx-large;
    transition: color 0.6s cubic-bezier(0.22, 1, 0.36, 1);
  }

  ul {
    margin: 0.25rem 0;
    padding: 0 0.25rem;

    list-style-type: none;
    gap: 10px;
  }

  li {
    display: flex;
  }
</style>
